import { createUseStyles } from 'react-jss'
import { base } from '../../../css/base'

export default createUseStyles({
    postList: {
        display: 'flex',
        flexWrap: 'wrap',
        padding: base()
    }
})